apiVersion: v1
kind: Service
metadata:
  name: web-service
  labels:
    app: synergychat-web
spec:
  selector:
    app: synergychat-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
